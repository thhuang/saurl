version: "3"
services:
  nginx:
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./nginx
    ports:
    - '16384:80'
  api:
    build:
      dockerfile: Dockerfile
      context: ./api
    volumes:
      - ./api:/app
  client:
    build:
      dockerfile: Dockerfile
      context: ./client
    volumes:
    - /app/node_modules
    - ./client:/app